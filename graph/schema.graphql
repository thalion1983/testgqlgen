type Mutation {
	createUser(input: NewUser!): User!
	removeUser(id: Int!): User!
}

type Query {
	users: [User!]!
}

input NewUser {
	name: String!
	age: Int!
}

type User {
	id: Int!
	name: String!
	age: Int!
}
